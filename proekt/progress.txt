## 2026-04-06 — TASK-018: Unit-тест для GET /api/v1/tax-limits

**Статус**: DONE

**Что сделано**:
- Создан тест-файл `backend/tests/test_tax_limits/test_tax_limits_router.py` (7 тестов)
- Тест использует минимальное FastAPI-приложение с только роутером tax_limits (без импорта app.main — избегает цепочки тяжёлых зависимостей)
- Исправлен баг в `app/routers/tax_limits.py`: `str(category)` заменён на `category.value` для корректной работы на Python 3.10/3.11
- Исправлен импорт в роутере (ruff --fix): Optional → `int | None`, порядок импортов

**Acceptance Criteria выполнены**:
- ✅ year=2023 → combined_limit=120000
- ✅ year=2024 → combined_limit=150000, 4 категории в limits[]
- ✅ treatment_expensive: is_uncapped=true, limit_amount=null, refund_percent=13
- ✅ year=2025 → 200 OK, combined_limit=150000
- ✅ без year → 422
- ✅ 7 passed, 0 failed

**Следующие задачи**:
- Все TASK-001..TASK-022 теперь done — redesign дашборда завершён

---

## 2026-04-03 — TASK-017: Замена эмодзи в HeroCard на Lucide Icons

**Статус**: DONE

**Что сделано**:
- В `frontend/src/app/(cabinet)/dashboard/page.tsx` добавлен импорт `Pill, Calendar, Banknote, ReceiptText, ClipboardList, Download, LucideIcon` из `lucide-react`
- Аватар-блок: `💊` → `<Pill size={32} color="#fff" strokeWidth={1.75} />`
- Компонент `FieldInfo`: тип пропса `icon` изменён с `string` на `LucideIcon`, рендеринг `{icon}` → `<Icon size={15} color="var(--accent)" strokeWidth={1.75} />`
- `📅` → `Calendar`, `💰` → `Banknote`, `🧾` → `ReceiptText`, `📋` → `ClipboardList`
- Кнопка «Скачать документы»: `📥` → `<Download size={14} />`

**Acceptance Criteria выполнены**:
- ✅ Все эмодзи в HeroCard заменены на SVG-иконки из lucide-react
- ✅ Иконки вписаны в существующий дизайн (accent цвет, размеры соответствуют)
- ✅ Нет регрессий: структура компонента не изменилась, только тип иконки

**Следующие задачи**:
- TASK-018: unit-тест для GET /api/v1/tax-limits (low, depends on TASK-002=done)

---

## 2026-04-03 — TASK-007: Расширяемый список вычетов в sidebar

**Статус**: DONE

**Что сделано**:
- В `frontend/src/app/(cabinet)/layout.tsx` добавлен интерфейс `DeductionType` и конфиг `DEDUCTION_TYPES: DeductionType[]`
- Конфиг содержит один элемент: `{ key: "social", label: "Социальный вычет", href: "/dashboard", icon: <HeartPulse />, available: true }`
- Создан компонент `DeductionNav({ activeKey })` — рендерит список из `DEDUCTION_TYPES`:
  - Активный элемент (key === activeKey) выделен классом `nav-item active` + синяя точка справа
  - Недоступные (available=false) — полупрозрачные, с бейджем «скоро», cursor: not-allowed
  - Добавление нового типа = только добавление объекта в `DEDUCTION_TYPES[]`
- Компонент вставлен в `Sidebar` между логотипом и основными nav-группами с заголовком «Тип вычета»
- Импортирован `HeartPulse` из `lucide-react`
- `pytest tests/test_deduction/` — 54 passed
- ruff ошибки в backend — pre-existing, не связаны с изменением

**Acceptance Criteria выполнены**:
- ✅ Компонент навигации принимает массив типов вычетов через конфиг `DEDUCTION_TYPES`
- ✅ Отображается только «Социальный вычет» (активный)
- ✅ Добавление нового типа — только через элемент в конфиге
- ✅ Активный тип визуально выделен (active класс + точка-индикатор)

**Следующие задачи**:
- TASK-017: заменить эмодзи в HeroCard на Lucide Icons (low)
- TASK-018: unit-тест для GET /api/v1/tax-limits (low)

---

## 2026-04-03 — TASK-006: Кнопка «i» с popup в sidebar

**Статус**: DONE

**Что сделано**:
- В `frontend/src/app/(cabinet)/layout.tsx` добавлен компонент `InfoPopup`
- Импортированы: `Info` из `lucide-react`, `useEffect`, `useRef` из React, `Link` из Next.js
- `InfoPopup` — кнопка с иконкой `<Info size={13}>` рядом с заголовком «Социальный вычет»
- При клике открывается popup с описанием социального вычета (13% НДФЛ, лимит 150 000 ₽)
- Popup закрывается при клике вне (обработчик через `document.addEventListener("mousedown", ...)`)
- Внутри popup: ссылка «Подробнее →» ведёт на `/info/social-vychet` (страница из TASK-008=done)
- `pytest tests/test_deduction/` — 54 passed

**Acceptance Criteria выполнены**:
- ✅ Иконка «i» (Info из Lucide) рядом с «Социальный вычет»
- ✅ При клике popup с описанием НДФЛ и вычета
- ✅ Ссылка «Подробнее →» на /info/social-vychet
- ✅ Popup закрывается при клике вне

**Разблокировано**:
- TASK-007: расширяемый список вычетов (medium, depends on TASK-005=done)

**Следующие задачи**:
- TASK-007: расширяемый навигационный список вычетов (medium priority)
- TASK-017: заменить эмодзи в HeroCard на Lucide Icons (low)
- TASK-018: unit-тест для GET /api/v1/tax-limits (low)

---

## 2026-04-03 — TASK-005: Заменить «МедВычет» на «Социальный вычет» в sidebar

**Статус**: DONE

**Что сделано**:
- В `frontend/src/app/(cabinet)/layout.tsx` импортирован `BadgePercent` из `lucide-react`
- Заменён логотип: эмодзи 💊 → `<BadgePercent size={18} strokeWidth={2} />`
- Заменён текст «МедВычет» → «Социальный вычет» в `logo-name`
- Тег-линия «Налоговый вычет» оставлена (соответствует стилю)
- `pytest tests/test_deduction/` — 54 passed

**Acceptance Criteria выполнены**:
- ✅ Текст «МедВычет» заменён на «Социальный вычет»
- ✅ Иконка из Lucide Icons (BadgePercent) — не эмодзи
- ✅ Визуально вписано в стиль sidebar (`.logo-icon` класс, те же размеры)

**Разблокировано**:
- TASK-006: кнопка «i» с tooltip (depends on TASK-005=done)
- TASK-007: расширяемый список вычетов (depends on TASK-005=done)

**Следующие задачи**:
- TASK-006: добавить кнопку «i» с popup описания социального вычета (high priority)
- TASK-007: расширяемый навигационный список вычетов (medium priority)
- TASK-017: заменить эмодзи в HeroCard на Lucide Icons (low, depends on TASK-016=done)
- TASK-018: unit-тест для GET /api/v1/tax-limits (low)

---

## 2026-04-03 — TASK-016: Финальная сборка дашборда

**Статус**: DONE

**Что сделано**:
- `DashboardContent` уже содержал все 4 панели: HeroCard, YearFilter, LimitsPanel, DocumentsPanel — структура была правильной
- Обнаружена и исправлена ошибка: `HeroCard` хардкодил `const year = new Date().getFullYear()` — год не менялся при переключении в `YearFilter`
- Добавлен проп `year: number` в `HeroCard` и `DashboardContent`
- `DashboardPage` передаёт `year` (из `useDashboardStore`) в `DashboardContent → HeroCard`
- Теперь все 4 компонента реагируют на смену `selectedYear`: HeroCard через пропс, LimitsPanel/DocumentsPanel через `useDashboardStore` напрямую
- `pytest tests/test_deduction/` — 54 passed

**Acceptance Criteria выполнены**:
- ✅ DashboardContent: HeroCard + YearFilter + LimitsPanel + DocumentsPanel
- ✅ Все компоненты используют единый selectedYear
- ✅ Смена года перерисовывает HeroCard (данные summary + год), LimitsPanel, DocumentsPanel
- ✅ Нет старых компонентов (StatusKanban, ReceiptsSchedule, SidePanel — удалены в TASK-015)

**Разблокировано**:
- TASK-017: заменить эмодзи в HeroCard на иконки Lucide (low priority, зависит от TASK-016)

**Следующие задачи**:
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)
- TASK-006: кнопка «i» с tooltip (high, зависит от TASK-005)
- TASK-007: расширяемый список вычетов в sidebar (medium, зависит от TASK-005)
- TASK-018: unit-тест для GET /api/v1/tax-limits (low, зависит от TASK-002=done)
- TASK-017: заменить эмодзи на Lucide Icons (low, зависит от TASK-016=done)

---

## 2026-04-03 — TASK-015: Удалить старые панели из дашборда

**Статус**: DONE

**Что сделано**:
- Удалены компоненты `StatusKanban`, `ReceiptsSchedule`, `SidePanel` из `frontend/src/app/(cabinet)/dashboard/page.tsx`
- Удалены вспомогательные функции `fmtDate`, `fmtDateFull`, `fmtMonth` (использовались только в удалённых компонентах)
- Удалена типизация `KanbanCard` interface
- Из `DashboardContent` удалены: `useQuery` для receipts-list, переменные `allReceipts`, `recentReceipts`, а также JSX этих компонентов
- Из импортов удалены: `useRouter`, `useQuery`, `api`, `MonthSummary`, `ReceiptListItem`, `ReceiptListResponse`
- `DashboardContent` теперь содержит только: HeroCard, YearFilter, LimitsPanel, DocumentsPanel
- `pytest tests/test_deduction/` — 54 passed
- ruff ошибки в backend — pre-existing, не связаны с изменением

**Разблокировано**:
- TASK-016: финальная сборка дашборда — все зависимости теперь done (TASK-009 ✓, TASK-012 ✓, TASK-014 ✓, TASK-015 ✓)

**Следующие задачи**:
- TASK-016: финальная сборка дашборда (critical, ВСЕ зависимости done) — **следующий приоритет**
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)
- TASK-006: кнопка «i» с tooltip (high, зависит от TASK-005)

---

## 2026-04-03 — TASK-014: DocumentsPanel (Панель 4)

**Статус**: DONE

**Что сделано**:
- Создан компонент `frontend/src/components/ui/DocumentsPanel.tsx`
- 5 групп документов с иконками из lucide-react:
  - `clinic_certs` — Справки из клиник (Stethoscope)
  - `recipes` — Рецепты (ScrollText)
  - `pharmacy_receipts` — Чеки из аптек (ShoppingBag)
  - `vhi_docs` — Документы ДМС (ShieldCheck)
  - `ndfl_certs` — Справки 2-НДФЛ (FileSpreadsheet)
- Данные берутся из `useDocumentStats(selectedYear)` — запрос `/api/v1/documents/stats?year={year}`
- `selectedYear` из `useDashboardStore` (Zustand) — при смене года React Query автоматически перезапрашивает через `queryKey: ["document-stats", year]`
- Скелетон при загрузке (5 пульсирующих placeholders)
- Заголовок: «Мои документы», год, бейдж «N на проверке» при `totalPending > 0`
- Легенда: синий квадрат = Подтверждено, жёлтый = На проверке
- Компонент добавлен в `DashboardContent` в `page.tsx` после `LimitsPanel`
- `pytest tests/test_deduction/` — 54 passed

**Разблокировано**:
- TASK-016: финальная сборка дашборда (осталось TASK-015)
- Критический путь: TASK-015 → TASK-016

**Следующие задачи**:
- TASK-015: удалить старые панели StatusKanban, ReceiptsSchedule, SidePanel (medium, нет зависимостей)
- TASK-016: финальная сборка дашборда (critical, осталась зависимость TASK-015)
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)

---

## 2026-04-03 — TASK-013: DocumentGroup (Панель 4)

**Статус**: DONE

**Что сделано**:
- Создан компонент `frontend/src/components/ui/DocumentGroup.tsx`
- Пропсы: `title`, `description`, `icon: LucideIcon`, `uploadedCount`, `pendingCount`, `groupKey`, `year`
- Иконка — в квадрате с акцентным фоном `var(--accent-light)`, цвет `var(--accent)`
- `uploadedCount > 0` → `<Link>` на `/receipts?type={groupKey}&status=confirmed&year={year}`, стиль акцентный
- `pendingCount > 0` → `<Link>` на `/receipts?type={groupKey}&status=pending&year={year}`, стиль warning
- При `count=0` → обычный `<span>` без ссылки
- Статус-индикатор: ✅ (uploaded>0, pending=0) | ⚠️ (pending>0) | ○ (оба=0)
- Все стили через CSS-переменные HEITKAMP: `var(--accent)`, `var(--text-muted)`, `var(--warning)` и т.д.

**Разблокировано**:
- TASK-014: DocumentsPanel (теперь TASK-013 done, TASK-009 done, TASK-022 done — все зависимости выполнены)
- TASK-016: после TASK-014 + TASK-015 → финальная сборка дашборда

**Следующие задачи**:
- TASK-014: собрать DocumentsPanel из 5 групп (high, все зависимости done)
- TASK-015: удалить старые панели StatusKanban, ReceiptsSchedule, SidePanel (medium, нет зависимостей)
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)

---

## 2026-04-03 — TASK-012: LimitsPanel (Панель 3)

**Статус**: DONE

**Что сделано**:
- Создан компонент `frontend/src/components/ui/LimitsPanel.tsx`
- Компонент берёт `selectedYear` из `useDashboardStore`
- Данные лимитов: `useTaxLimits(selectedYear)` — `TaxLimitsResponse`
- Данные расходов: `useExpenseCategories(selectedYear)` — `CategoryBreakdown`
- Отображает `BigGlass` (общий лимит, синяя+зелёная зоны) + 4 `SmallGlass` (по категориям)
- Цветовая схема: treatment_regular=#4A8FE2 (синий), treatment_expensive=#7B9FD4 (нейтральный синий), education_self=#3BAB72 (зелёный), education_child=#9B7FD4 (фиолетовый)
- Цвета зон BigGlass совпадают с цветами соответствующих SmallGlass (treatment=#4A8FE2, education=#3BAB72)
- При смене года React Query автоматически перезапрашивает через `queryKey: ["tax-limits", year]` и `["expense-categories", year]`
- LimitsPanel добавлена в DashboardContent после YearFilter
- Заголовок панели: «Налоговые лимиты» + год + суммарный лимит
- Скелетон при загрузке (пульсирующий placeholder)
- Для безлимитных (treatment_expensive): возврат = spent × 13% (динамический расчёт)
- pytest tests/test_deduction/ — 54 passed

**Разблокировано**:
- TASK-016: сборка дашборда (осталось TASK-013, TASK-014, TASK-015)
- Критический путь: TASK-013 → TASK-014 → TASK-016

**Следующие задачи**:
- TASK-013: DocumentGroup компонент (high, TASK-004 done)
- TASK-015: удалить старые панели (medium, нет зависимостей)
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)

---

## 2026-04-03 — TASK-010: Компонент BigGlass (Панель 3)

**Статус**: DONE

**Что сделано**:
- Создан компонент `frontend/src/components/ui/BigGlass.tsx`
- Пропсы: `totalLimit`, `treatmentAmount`, `educationAmount`
- Размер 120×140px — шире и выше SmallGlass (72×80px)
- Две цветные зоны: синяя (#4A8FE2, лечение) снизу, зелёная (#3BAB72, обучение) поверх
- Высота каждой зоны пропорциональна `amount/totalLimit`, с анимацией `height`/`bottom` 600ms
- Над стаканом: лейбл «Общий лимит», «{потрачено} / {лимит}», процент заполнения
- Легенда снизу с цветными квадратиками и подписями
- Hover-tooltip: цветные точки для каждой зоны + итог
- Тест-кейс: treatment=60000, education=40000, limit=150000 → синяя 40%, зелёная ~27%, итого ~67%
- При нулях: стакан пустой, процент «0%» серым

**Разблокировано**:
- TASK-012: LimitsPanel (BigGlass + SmallGlass + зависимости — теперь все done)
- Критический путь: TASK-012 → TASK-016

**Следующие задачи**:
- TASK-012: собрать LimitsPanel из BigGlass + 4 SmallGlass (высокий, все зависимости done)
- TASK-013: DocumentGroup компонент (высокий, TASK-004 done)
- TASK-015: удалить старые панели (medium, нет зависимостей)

---

## 2026-04-03 — TASK-011: Компонент SmallGlass (Панель 3)

**Статус**: DONE

**Что сделано**:
- Создан компонент `frontend/src/components/ui/SmallGlass.tsx`
- Пропсы: `categoryName`, `color`, `spent`, `limit`, `refund`, `isUncapped`
- Для `isUncapped=false`: двухслойная заливка (opacity фон + gradient) пропорционально `spent/limit`, с анимацией через CSS transition `height`
- Для `isUncapped=true`: пустой стакан + анимированный символ ∞ (keyframe `smallGlassPulse`)
- Над стаканом: процент заполнения (`Math.round(%)%`) или `—` для безлимитных
- Под стаканом: название, лимит (форматирован в ₽ через `Intl.NumberFormat`) или ∞, макс. возврат
- Hover-tooltip: название, потрачено, лимит/безлимит, макс. возврат. Стрелочка через CSS rotate
- Все стили через CSS-переменные (`var(--accent)`, `var(--border-strong)`, и т.д.)
- 54 deduction теста прошли, ruff errors — pre-existing, не связаны с изменением

**Разблокировано**:
- TASK-010: BigGlass (теперь TASK-011 done — оба компонента готовы к сборке в TASK-012)
- Критический путь: TASK-010 → TASK-012 → TASK-016

**Следующие задачи**:
- TASK-010: BigGlass компонент (высокий приоритет, зависимости TASK-003+TASK-004 done)
- TASK-013: DocumentGroup (высокий приоритет, TASK-004 done)
- TASK-015: удалить старые панели (medium, нет зависимостей)

---

## 2026-04-03 — TASK-022: Frontend хуки useExpenseCategories и useDocumentStats

**Статус**: DONE

**Что сделано**:
- Типы `CategoryBreakdown`, `ExpenseCategoryItem` добавлены в `frontend/src/types/api.ts` (были уже присутствовали)
- Типы `DocumentStats`, `DocumentStatGroup` добавлены в `frontend/src/types/api.ts` (были уже присутствовали)
- Создан хук `frontend/src/hooks/useExpenseCategories.ts`:
  - `useExpenseCategories(year: number)` → React Query, `queryKey: ["expense-categories", year]`
  - `staleTime: 60_000` (1 минута ≥ 60 сек)
  - Запрашивает `GET /api/v1/expenses/categories?year={year}`
- Создан хук `frontend/src/hooks/useDocumentStats.ts`:
  - `useDocumentStats(year: number)` → React Query, `queryKey: ["document-stats", year]`
  - `staleTime: 60_000`
  - Запрашивает `GET /api/v1/documents/stats?year={year}`
- Оба хука имеют `year` в `queryKey` — React Query автоматически перезапрашивает при смене года

**Разблокировано**:
- TASK-012: LimitsPanel (теперь TASK-022 done, осталось TASK-010 и TASK-011)
- TASK-014: DocumentsPanel (теперь TASK-022 done, осталось TASK-013)
- Критический путь: TASK-011 → TASK-010 → TASK-012 → TASK-016

**Следующие задачи**:
- TASK-011: SmallGlass компонент (нет зависимостей, high priority) — **рекомендуется следующей**
- TASK-013: DocumentGroup компонент (TASK-004 done)
- TASK-010: BigGlass компонент (все зависимости done)
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)

---

## 2026-04-03 — TASK-021: Backend GET /api/v1/documents/stats

**Статус**: DONE

**Что сделано**:
- Создан роутер `backend/app/routers/documents.py` с эндпоинтом `GET /api/v1/documents/stats?year={year}`
- Эндпоинт требует авторизации (JWT-кука через `get_current_user`)
- Возвращает 5 групп: `clinic_certs`, `recipes`, `pharmacy_receipts`, `vhi_docs`, `ndfl_certs`
- Источники данных:
  - `clinic_certs`: Prescription с doc_type в DOC_* + Document.CLINIC_CERT
  - `recipes`: Prescription с doc_type RECIPE_107/RECIPE_EGISZ
  - `pharmacy_receipts`: Receipt (фильтр по `purchase_date` год)
  - `vhi_docs`: Document.VHI_CERT
  - `ndfl_certs`: Document.NDFL_2
- `pending_count` для Receipt = ocr_status in (PENDING, REVIEW, DUPLICATE_REVIEW)
- `pending_count` для Document = doc_status = PENDING
- `pending_count` для Prescription = duplicate_of_id IS NOT NULL
- Роутер зарегистрирован в `app/main.py`
- `ruff check` для новых файлов — чисто; `pytest tests/test_deduction/` — 54 passed

**Разблокировано**:
- TASK-022: Frontend хуки `useExpenseCategories` и `useDocumentStats` (теперь оба бекенд-эндпоинта готовы)
- После TASK-022 → TASK-012 + TASK-014 → TASK-016 (критический путь)

**Следующие задачи**:
- TASK-022: создать типы и хуки `useExpenseCategories(year)` и `useDocumentStats(year)` (высокий приоритет, обе зависимости done)
- TASK-011: SmallGlass компонент (нет зависимостей, high priority)
- TASK-010: BigGlass компонент (зависимости выполнены)

---

## 2026-04-03 — TASK-020: Модель Document + миграция БД

**Статус**: DONE

**Что сделано**:
- Добавлены enum `DocumentType` (clinic_cert, vhi_cert, ndfl_2, contract) и `DocumentStatus` (uploaded, pending, confirmed) в `backend/app/models/enums.py`
- Создана модель `Document` в `backend/app/models/document.py` с полями: id, user_id, doc_type, doc_status, tax_year, s3_key, filename, created_at
- Добавлен relationship `documents` в `User` (cascade all, delete-orphan)
- Зарегистрировано в `backend/app/models/__init__.py`
- Создана миграция Alembic `0011_document_model.py` (revision=0011, down_revision=0010)
  - CREATE TYPE documenttype, documentstatus
  - CREATE TABLE documents + индексы по user_id и (user_id, tax_year)
  - downgrade: DROP TABLE + DROP TYPE

**Разблокировано**:
- TASK-021: Backend эндпоинт GET /api/v1/documents/stats?year={year} (теперь модель есть)
- После TASK-021 → TASK-022 → TASK-014 → TASK-016 (критический путь)

**Следующие задачи**:
- TASK-021: создать эндпоинт /api/v1/documents/stats (depends on TASK-020=done теперь)
- TASK-011: SmallGlass компонент (no deps, high priority, frontend)
- TASK-010: BigGlass компонент (TASK-003+TASK-004 done, high priority)

---

## 2026-04-03 — TASK-008: Страница /info/social-vychet

**Статус**: DONE

**Что сделано**:
- Создана страница `frontend/src/app/info/social-vychet/page.tsx`
- Server Component (без "use client"), статическая страница
- Хлебные крошки: Дашборд → Социальный вычет
- Hero-баннер с градиентом в стиле HEITKAMP + бейдж «Статья 219 НК РФ»
- KPI-блок: ставка 13%, лимит 150 000 ₽, макс. возврат 19 500 ₽
- Раздел «Что такое вычет» с примером расчёта и checklist-ом
- 5 карточек категорий: лечение, лекарства, обучение своё/ребёнка, дорогостоящее
- Таблица лимитов по годам (2023–2025), с подсветкой актуального года
- Раздел «Необходимые документы» (2 колонки: лекарства / лечение)
- CTA-баннер «Перейти в кабинет» со ссылкой на /dashboard
- Все иконки из lucide-react (без эмодзи), CSS-переменные из HEITKAMP

**Следующие задачи**:
- TASK-005: заменить «МедВычет» на «Социальный вычет» в sidebar (high, нет зависимостей)
- TASK-010: компонент BigGlass (high, зависимости выполнены)
- TASK-011: компонент SmallGlass (high, нет зависимостей)
- TASK-013: компонент DocumentGroup (high, TASK-004 done)

---

## 2026-04-03 — TASK-003: Хук useTaxLimits(year)

**Статус**: DONE

**Что сделано**:
- Добавлены типы `TaxLimitItem` и `TaxLimitsResponse` в `frontend/src/types/api.ts`
- Создан хук `frontend/src/hooks/useTaxLimits.ts`
- `useTaxLimits(year: number)` — React Query хук, запрашивает `GET /api/v1/tax-limits?year={year}`
- `queryKey: ["tax-limits", year]` — автоматический refetch при смене года
- `staleTime: 5 * 60_000` (5 минут, выполняет требование ≥5 мин)

**Следующий шаг**:
- TASK-009: создать `YearFilter` компонент — зависит от TASK-004 (done)
- TASK-010: создать `BigGlass` — теперь TASK-003 done, зависит ещё от TASK-004 (done) → TASK-010 готов к реализации
- TASK-011: создать `SmallGlass` — без зависимостей, готов к реализации

---

## 2026-04-03 — TASK-009: Компонент YearFilter (Панель 2)

**Статус**: DONE

**Что сделано**:
- Создан компонент `frontend/src/components/ui/YearFilter.tsx`
- Отображает 4 года динамически: `[currentYear, currentYear-1, currentYear-2, currentYear-3]`
- Активный год — акцентный фон `var(--accent)` + белый текст; неактивный — hover-эффект `var(--accent-light)`
- При клике вызывает `setSelectedYear` из `useDashboardStore` (TASK-004)
- Добавлен в `DashboardPage`: `DashboardContent` рендерит `<YearFilter />` после HeroCard
- `DashboardPage` теперь берёт `year` из `useDashboardStore` вместо `new Date().getFullYear()` — смена года перезапрашивает summary через React Query (queryKey `["summary", year]`)

**Следующие задачи (разблокированы)**:
- TASK-012: LimitsPanel — теперь TASK-009 done (нужны ещё TASK-010, TASK-011, TASK-022)
- TASK-013: DocumentGroup — зависит от TASK-004 (done), готов к реализации
- TASK-014: DocumentsPanel — зависит от TASK-013, TASK-009 (теперь done), TASK-022

---

## 2026-04-03 — TASK-004: Глобальный year state (Zustand)

**Статус**: DONE

**Что сделано**:
- Добавлен `dashboardStore` в `frontend/src/lib/store.ts`
- `selectedYear: number` — по умолчанию `new Date().getFullYear()` (текущий год)
- `setSelectedYear(year: number)` — экшен для смены года
- Экспортируется как `useDashboardStore`

**Архитектурное решение**:
React Query invalidation при смене года не нужна явно — хуки будут включать `year` в query key (например `['tax-limits', year]`), и RQ автоматически перезапрашивает при изменении year.

**Следующий шаг**:
- TASK-003: создать хук `useTaxLimits(year)` — зависит от TASK-002 (done)
- TASK-009: создать `YearFilter` компонент — зависит от TASK-004 (теперь done)
- TASK-013: создать `DocumentGroup` компонент — зависит от TASK-004 (теперь done)
