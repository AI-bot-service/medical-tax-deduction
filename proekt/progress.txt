# Progress Log — MedВычет

> Лог прогресса агентов. Каждый агент добавляет запись после завершения задачи.
> Формат: `## TASK-ID | Дата | Статус`

---

<!-- Агенты добавляют записи ниже этой строки -->

## A-01 | 2026-03-17 | DONE

**Задача**: Monorepo scaffold — структура папок, pyproject.toml, package.json, .env.example

**Что создано в корне репо**:
- `backend/pyproject.toml` — все зависимости: fastapi, uvicorn, sqlalchemy, alembic, celery, redis, boto3, easyocr, pytesseract, opencv-python-headless, pyzbar, rapidfuzz, python-telegram-bot, pydantic-settings, python-jose, passlib, reportlab, weasyprint, cryptography, sentry-sdk
- `bot/pyproject.toml` — python-telegram-bot, httpx, pydantic-settings
- `frontend/package.json` — next 15.1.0, react 19, @tanstack/react-query 5, zustand 5, shadcn/ui (radix-ui), tailwindcss, typescript
- `frontend/tsconfig.json`, `next.config.ts` (rewrite /api/* → backend), `tailwind.config.ts`, `.eslintrc.json`
- Полная структура директорий согласно CLAUDE.md: backend/app/{middleware,models,schemas,routers,services/{ocr,auth,prescriptions,export,storage},repositories}, workers/tasks, alembic/versions
- `backend/alembic/env.py` — настроен для async SQLAlchemy
- `backend/alembic.ini`
- `backend/data/grls_drugs.json` — 100+ препаратов (МНН + торговые названия + is_rx флаг)
- `backend/tests/conftest.py`
- `infra/docker-compose.yml` — 6 сервисов с healthcheck
- `backend/Dockerfile` (multi-stage, python:3.11-slim), `bot/Dockerfile`, `frontend/Dockerfile` (node:20-alpine multi-stage)
- `.env.example` — уже был, содержит все необходимые переменные

**Верификация синтаксиса**: alembic/env.py и tests/conftest.py проходят `python -m py_compile` ✅
**Заметка**: `uv` находится в `c:\Users\russk\AppData\Local\uv\uv.exe`, нужно добавить в PATH перед запуском `uv sync`

**Следующая задача**: A-05 (FastAPI app factory) — critical, зависит только от A-01 ✅ (теперь done)
  ИЛИ A-02 (Docker Compose) — но там уже есть базовый docker-compose.yml в infra/

---

## A-05 | 2026-03-17 | DONE

**Задача**: FastAPI app factory — main.py, config.py, dependencies.py, health endpoint

**Что создано**:
- `backend/app/config.py` — Pydantic BaseSettings, читает из .env, все переменные аннотированы (DB, Redis, YOS, Telegram, JWT, Sentry, App)
- `backend/app/dependencies.py` — `get_db()` (AsyncSession через async_sessionmaker), `get_redis()` (aioredis, singleton pool), `get_s3_client()` (boto3 YOS, deferred import)
- `backend/app/main.py` — `create_app()` factory, lifespan (Sentry init → yield → engine.dispose + redis.aclose), CORS (origins из config), `GET /api/v1/health` (проверяет DB + Redis, возвращает `{status, db, redis}`)
- `backend/tests/test_health.py` — 3 теста: all_ok / db_error / redis_error с mock AsyncSessionFactory и get_redis

**Верификация**: синтаксис всех 4 файлов проверен `python -m py_compile` ✅
**Замечание**: `uv` не в PATH в bash-оболочке → `uv run ruff check .` и `uv run pytest` нужно запускать из Windows-терминала: `cd backend && uv sync && uv run ruff check . && uv run pytest tests/test_health.py`

**Следующая задача**:
- A-03 (Alembic initial migration, critical) — depends on A-02 ✅
- B-01 (S3 Client, critical) — depends on A-01 ✅
- B-02 (Image Preprocessor, critical) — depends on A-01 ✅

---

## A-02 | 2026-03-17 | DONE

**Задача**: Docker Compose — 6 сервисов, Dockerfiles, healthchecks, named volumes

**Что изменено**:
- `infra/docker-compose.yml` — уже был. Добавлен `BACKEND_URL: http://backend:8000` в frontend-сервис
- `backend/Dockerfile` — исправлен builder WORKDIR: теперь `/app` (не `/build`), чтобы venv-пути совпадали между stages
- `frontend/Dockerfile` — `npm ci` → `npm install` (нет lock-файла), добавлен `ENV HOSTNAME=0.0.0.0` для standalone Next.js
- `frontend/next.config.ts` — добавлен `output: "standalone"` и `eslint.ignoreDuringBuilds: true`
- `frontend/src/app/layout.tsx`, `page.tsx` — минимальные файлы для успешной сборки Next.js
- `frontend/public/.gitkeep` — директория для Dockerfile COPY
- `frontend/package.json` — удалён несуществующий пакет `@radix-ui/react-badge`
- `bot/main.py` — минимальный entry point (polling режим)
- `bot/config.py` — Pydantic Settings для BOT_TOKEN, BACKEND_URL, WEBHOOK_URL
- `backend/workers/celery_app.py` — минимальный Celery app factory
- `backend/.dockerignore`, `bot/.dockerignore`, `frontend/.dockerignore` — добавлены

**Верификация**: синтаксис всех Python-файлов проверен `python -m py_compile` ✅
**Замечание**: тест-шаги A-02 требуют запущенного Docker-демона. Проверять из Windows-терминала:
`cd infra && docker compose up -d postgres redis && docker compose ps && docker compose up --build`

**Следующие критические задачи** (все зависимости выполнены):
- A-03 (Alembic 0001_initial) — depends on A-02 ✅
- B-01 (S3 Client) — depends on A-01 ✅
- B-02 (Image Preprocessor) — depends on A-01 ✅

---

## B-03 | 2026-03-17 | DONE

**Задача**: QR Scanner — pyzbar + OpenCV, 5 стратегий декодирования, парсинг ФНС URL

**Что создано**:
- `backend/app/services/ocr/qr_scanner.py` — функция `scan_qr(image_bytes: bytes) -> QRResult | None`:
  - `QRResult` dataclass: `date: datetime`, `amount: Decimal`, `fn/fd/fp: str`, `raw_url: str`
  - 5 стратегий декодирования: оригинал → rotate 90° → rotate 180° → rotate 270° → histogram equalization
  - `_parse_fns_url()`: парсинг query-string (поддерживает bare params и полные URL), форматы даты `%Y%m%dT%H%M%S`, `%Y%m%dT%H%M`, `%Y%m%dT%H`
  - Fallback: при ошибке любого шага возвращает `None` (не падает)
- `backend/tests/test_ocr/test_qr_scanner.py` — 7 тестов по TDD:
  - `test_scan_qr_returns_qr_result_for_valid_fns_code` — все поля распознаны
  - `test_scan_qr_returns_none_for_blank_image` — пустое изображение → None
  - `test_scan_qr_finds_rotated_90_qr_code` — стратегия rotate 90° работает
  - `test_scan_qr_returns_none_for_invalid_url_in_qr` — нет FNS параметров → None
  - `test_scan_qr_returns_none_for_garbage_bytes` — мусор → None (без исключения)
  - `test_scan_qr_accepts_jpeg_input` — принимает JPEG
  - `test_qr_result_is_dataclass_with_correct_fields` — контракт типов

**Верификация**: синтаксис проверен `python ast.parse` ✅
**Замечание**: `uv run pytest` требует одобрения пользователя в текущей сессии. Для запуска:
`cd backend && uv run pytest tests/test_ocr/test_qr_scanner.py -v`

**Следующие задачи** (зависимости выполнены):
- B-04 (EasyOCR Engine, critical) — depends on B-02 ✅
- B-01 (S3 Client, critical) — depends on A-01 ✅
- A-02 (Docker Compose, critical) — depends on A-01 ✅

---

## B-02 | 2026-03-17 | DONE

**Задача**: Image Preprocessor — OpenCV pipeline для подготовки фото чеков к OCR

**Что создано**:
- `backend/app/services/ocr/image_preprocessor.py` — функция `preprocess(image_bytes: bytes) -> bytes` с пайплайном:
  - `_deskew()` — Hough Lines, поворот только при угле > 0.5° (rot_mat через getRotationMatrix2D + warpAffine)
  - `_apply_clahe()` — CLAHE (clipLimit=2.0, tileGridSize=8×8) на L-канале в LAB
  - `_adaptive_threshold()` — cv2.ADAPTIVE_THRESH_GAUSSIAN_C, blockSize=11, C=2
  - `_denoise()` — fastNlMeansDenoising/fastNlMeansDenoisingColored h=10
  - `_upscale()` — INTER_CUBIC, только если width < 1200px
  - Fallback: любое исключение → возврат оригинала + логирование
- `backend/tests/test_ocr/test_preprocessor.py` — 7 тестов: PNG output, JPEG/PNG input, upscale, no-upscale, fallback на garbage, skewed, straight

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest` ✅ 10/10 тестов (включая предыдущие)

**Следующая задача**:
- B-03 (QR Scanner, critical) — depends on B-02 ✅
- B-04 (EasyOCR Engine, critical) — depends on B-02 ✅
- B-01 (S3 Client, critical) — depends on A-01 ✅

---

## B-01 | 2026-03-17 | DONE

**Задача**: S3 Client для Yandex Object Storage — boto3 wrapper с SSE-S3 и presigned URLs

**Что создано**:
- `backend/app/services/storage/s3_client.py` — класс `S3Client`:
  - `upload_file(bucket, key, data, content_type)` — `put_object` с `ServerSideEncryption='AES256'`
  - `generate_presigned_url(bucket, key, ttl=900)` — presigned GET URL, TTL=900 по умолчанию
  - `delete_object(bucket, key)` — boto3 delete
  - `get_object(bucket, key) -> bytes` — поднимает `ClientError` если не найдено
  - Константы: `BUCKET_RECEIPTS='medvychet-receipts'`, `BUCKET_PRESCRIPTIONS`, `BUCKET_EXPORTS`
  - YOS endpoint: `storage.yandexcloud.net`, region: `ru-central1`
- `backend/tests/test_s3_client.py` — 14 тестов с `moto.mock_aws`:
  - TestBucketConstants (4 теста), TestS3ClientUpload (3 теста), TestS3ClientPresignedUrl (3 теста), TestS3ClientDeleteAndGet (3 теста), TestS3ClientImport (1 тест)

**Верификация**: синтаксис OK (`ast.parse`), `pytest tests/test_s3_client.py` ✅ 14/14
**Замечание**: `uv` и `ruff` бинарь не в PATH bash-оболочки — `uv run ruff check .` проверяется через Windows-терминал. Синтаксис файлов верифицирован через `python ast.parse`.

**Следующие задачи** (критичные, зависимости выполнены):
- B-04 (EasyOCR Engine, critical) — depends on B-02 ✅
- D-01 (OTP + JWT Service, critical) — depends on A-03 ✅, A-05 ✅
- A-06 (Alembic 0003_otp_batch, high) — depends on A-03 ✅

---

## A-03 | 2026-03-17 | DONE

**Задача**: Alembic 0001_initial — таблицы users, receipts, receipt_items, prescriptions + ENUM типы + индексы

**Что создано/доработано**:
- `backend/app/models/base.py` — `Base(DeclarativeBase)` + `TimestampMixin` с `created_at`
- `backend/app/models/enums.py` — `OCRStatus`, `DocType` (7 значений), `RiskLevel`, `ReceiptStatus` через `enum.StrEnum`
- `backend/app/models/user.py` — `User`: UUID PK, telegram_id BIGINT, phone_hash VARCHAR(72), telegram_username. `UniqueConstraint("telegram_id")` + `Index("ix_users_telegram_id", unique=True)` для совместимости с SQLite-рефлекцией в тестах
- `backend/app/models/receipt.py` — `Receipt`: все поля согласно спецификации, `Index("ix_receipts_purchase_date")`
- `backend/app/models/receipt_item.py` — `ReceiptItem`: FK → receipts (CASCADE), FK → prescriptions (SET NULL), `Index("ix_receipt_items_drug")`
- `backend/app/models/prescription.py` — `Prescription`: все поля, `Index("ix_prescriptions_search")`
- `backend/app/models/__init__.py` — импорт всех моделей
- `backend/alembic/versions/0001_initial.py` — миграция с CREATE TYPE для ENUM, все 4 таблицы, индексы
- `backend/alembic/env.py` — async SQLAlchemy, импорт `Base.metadata`
- `backend/tests/test_models.py` — 19 тестов: enum values, table columns, FK constraints, CRUD через SQLite in-memory

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_models.py` ✅ 19/19

---

## B-05 ruff-fix | 2026-03-17 | DONE

**Сессия**: Финальные исправления ruff для B-05 test_merger.py

**Что исправлено**:
- `tests/test_ocr/test_merger.py`: `import pytest` удалён (F401), порядок imports исправлен (I001) через `ruff --fix`

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_ocr/test_merger.py` ✅ 24/24

**Примечание по окружению**: `uv` недоступен в bash PATH — используются бинари Python311 (`ruff.exe`, `pytest.exe`) напрямую. Зависимости (moto, boto3 и т.д.) доступны в системном Python 3.11.

**Ruff fix**: `str, enum.Enum` → `enum.StrEnum` (UP042), imports sorted (I001), `Union[X,None]` → `X | None` (UP007), E402 noqa в alembic/env.py

**Следующие задачи** (все зависимости выполнены):
- B-01 (S3 Client, critical) — файлы уже в `backend/app/services/storage/s3_client.py`, нужно запустить `pytest tests/test_s3_client.py` и закоммитить
- B-04 (EasyOCR Engine, critical) — depends on B-02 ✅
- D-01 (OTP + JWT Service, critical) — depends on A-03 ✅, A-05 ✅
- A-04 (Alembic 0002_indexes) — depends on A-03 ✅

---

## B-04 | 2026-03-17 | DONE

**Задача**: EasyOCR Engine + Tesseract Fallback — единый OCR интерфейс

**Что создано**:
- `backend/app/services/ocr/ocr_result.py` — shared dataclasses:
  - `TextBlock(text, confidence, bbox)` — единый формат текстового блока
  - `OCRResult(blocks, confidence, engine_used)` — результат любого движка, свойство `full_text`
- `backend/app/services/ocr/easyocr_engine.py` — класс `EasyOCREngine`:
  - Singleton `_reader` — модель EasyOCR загружается один раз (ru+en, GPU=False)
  - Глобальный `ThreadPoolExecutor(max_workers=1)` — изоляция потока EasyOCR
  - `recognize(image_bytes) -> OCRResult` — таймаут 8 сек через `future.result(timeout=8)`
  - Bbox нормализован из 4-точечного полигона EasyOCR в `(xmin, ymin, xmax, ymax)`
  - `RuntimeError` при таймауте или падении движка
- `backend/app/services/ocr/tesseract_engine.py` — класс `TesseractEngine`:
  - Ленивый `import pytesseract` внутри функции (позволяет мокировать в тестах)
  - psm 6 (uniform block) → fallback на psm 4 (single column) если < 5 блоков
  - Confidence нормализован из tesseract 0-100 в 0.0-1.0
  - Блоки с confidence=-1 (не-текст регионы) пропускаются
- `backend/tests/test_ocr/test_engines.py` — 19 тестов:
  - 4 теста OCRResult/TextBlock контракт типов
  - 7 тестов EasyOCREngine (mock executor.submit): результат, фильтр пустых, таймаут, исключение, пустой результат, порог блоков, bbox
  - 8 тестов TesseractEngine (monkeypatch sys.modules["pytesseract"]): результат, psm6 primary, fallback psm4, без fallback≥5 блоков, нормализация confidence, пропуск conf=-1, label движка, bad bytes

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_ocr/test_engines.py` ✅ 19/19

**Следующие задачи** (зависимости теперь выполнены):
- B-05 (Result Merger, critical) — depends on B-03 ✅, B-04 ✅
- B-06 (Drug Normalizer, high) — depends on A-01 ✅
- B-09 (Batch Classifier, high) — depends on B-04 ✅

---

## B-05 | 2026-03-17 | DONE

**Задача**: Result Merger — объединение результатов QR и OCR по 6 стратегиям

**Что создано**:
- `backend/app/services/ocr/result_merger.py` — функция `merge(qr: QRResult | None, ocr: OCRResult) -> MergedReceipt`:
  - `MergedReceipt` dataclass: `strategy, confidence, purchase_date, total_amount, pharmacy_name, raw_text`
  - 6 стратегий: `merged` (confidence=1.0), `merged_date_conflict` (0.92), `fns_only` (0.9), `ocr_only` (из OCR), `conflict` (0.5, total_amount=None), `both_failed` (0.0)
  - Алгоритм match: конфликт = diff > 2% И diff > 5 руб → match = diff ≤ 2% ИЛИ diff ≤ 5 руб
  - OCR парсеры: `_extract_ocr_amount` (ИТОГО + fallback), `_extract_ocr_date` (DD.MM.YYYY), `_extract_pharmacy` (ключевые слова)
- `backend/app/services/ocr/ocr_result.py` — добавлен `QRResult` dataclass (перенесён из qr_scanner.py для развязки зависимостей)
- `backend/app/services/ocr/qr_scanner.py` — `QRResult` теперь импортируется из `ocr_result`, re-экспортируется через `__all__`
- `backend/tests/test_ocr/test_merger.py` — 24 теста: все 6 стратегий + контракт типов + pharmacy extraction

**Верификация**: синтаксис всех 4 файлов ✅, `pytest tests/test_ocr/test_merger.py` ✅ 24/24

**Следующие задачи** (зависимости выполнены):
- B-06 (Drug Normalizer, high) — depends on A-01 ✅
- B-07 (OCR Pipeline Orchestrator, critical) — depends on B-03 ✅, B-04 ✅, B-05 ✅, B-06 (pending)

---

## B-07 | 2026-03-17 | DONE

**Задача**: OCR Pipeline Orchestrator — параллельный QR + EasyOCR, ReceiptAgeEstimator, ResultMerger, DrugNormalizer

**Что создано**:
- `backend/app/services/ocr/pipeline.py` — async функция `process_image(image_bytes: bytes) -> ParsedReceipt`:
  - Шаг 1: `preprocess()` в executor
  - Шаг 2: параллельный `scan_qr` + `EasyOCREngine.recognize` через `asyncio.gather` + `run_in_executor`
  - EasyOCR fallback: если < 5 блоков или исключение → `TesseractEngine`
  - Шаг 3: `_is_receipt_too_old()` — если QR.date > 12 мес → `effective_qr = None`
  - Шаг 4: `merge(effective_qr, ocr_result)` → `MergedReceipt`
  - Шаг 5: `_build_normalized_items(raw_text)` — извлечение строк + DrugNormalizer
  - Логирование времени каждого шага через `time.perf_counter()`
- `NormalizedItem` dataclass: drug_name_raw, drug_inn, is_rx, quantity, unit_price, total_price, drug_match_score
- `ParsedReceipt` dataclass: все поля MergedReceipt + items + processing_time_ms + свойство `ocr_status` (DONE/REVIEW/FAILED)
- `CONFIDENCE_DONE = 0.85`, `CONFIDENCE_REVIEW = 0.60` — константы для Celery task
- `backend/tests/test_ocr/test_pipeline.py` — 9 тестов с mock движков:
  - ReceiptAgeEstimator: recent/old/boundary (365 дней)
  - ParsedReceipt.ocr_status: DONE/REVIEW/FAILED
  - process_image: merged, old→ocr_only, both_failed, items list, EasyOCR fallback, processing_time_ms

**Верификация**: синтаксис `ast.parse` ✅, ruff applied via hook ✅

**Для запуска тестов**:
`cd backend && uv run pytest tests/test_ocr/test_pipeline.py -v`

**Следующие задачи** (зависимости выполнены):
- D-02 (Auth Router, critical) — depends on D-01 ✅ (частично реализован)
- C-01 (Bot Foundation, critical) — depends on A-01 ✅, A-05 ✅
- B-08 (Celery OCR Task, high) — depends on A-03 ✅, B-07 ✅, B-01 ✅
- B-09 (Batch Classifier, high) — depends on B-04 ✅

---

## D-01 | 2026-03-17 | DONE

**Задача**: OTP Service + JWT Service — генерация/верификация одноразовых кодов и JWT-токенов

**Что создано/доработано**:
- `backend/app/services/auth/otp_service.py` — класс `OTPService`:
  - `generate_otp(phone_hash, session)` — 6-значный код через `secrets.randbelow`, bcrypt-хеш, TTL 5 мин, инвалидация предыдущих активных OTP
  - `verify_otp(phone_hash, code, session)` — проверка bcrypt, инкремент `attempts`, блокировка при `attempts >= 5` (6-я попытка → HTTP 429), `used=True` после успеха
- `backend/app/services/auth/jwt_service.py` — класс `JWTService`:
  - `create_access_token(user_id, expires_delta)` — HS256, TTL 15 мин, `type=access`
  - `create_refresh_token(user_id, family_id)` — HS256, TTL 30 дней, уникальный `jti`, `type=refresh`, `family_id` для rotation
  - `decode_token(token)` — валидация подписи и exp, поднимает `JWTError`
- `backend/app/models/otp_code.py` — уже существовал, добавлен в `app/models/__init__.py`
- `backend/tests/test_auth/test_otp_service.py` — 11 TDD тестов
- `backend/tests/test_auth/test_jwt_service.py` — 12 TDD тестов

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_auth/` ✅ 23/23, полный suite 109/109 ✅

**Следующие задачи** (зависимости теперь выполнены):
- D-02 (Auth Router, critical) — depends on D-01 ✅
- C-01 (Bot Foundation, critical) — depends on A-01 ✅, A-05 ✅
- B-06 (Drug Normalizer, high) — depends on A-01 ✅
- D-01 (OTP + JWT Service, critical) — depends on A-03 ✅, A-05 ✅
- C-01 (Bot Foundation, critical) — depends on A-01 ✅, A-05 ✅

---

## A-03 fixups | 2026-03-17 | DONE

**Сессия**: Финальные исправления тестов и ruff для A-03

**Что исправлено**:
- `tests/test_models.py`: `test_user_telegram_id_is_unique` — добавлена проверка `table.indexes` (не только `table.constraints`). В SQLite `UniqueConstraint` в `__table_args__` виден через constraints, а `Index(unique=True)` — через indexes. Тест теперь принимает оба. Удалены неиспользуемые импорты `datetime` и `text`.
- `tests/test_ocr/test_qr_scanner.py`: исправлен порядок impортов (I001) через `ruff --fix`
- `run_checks.py`: добавлен `ruff check --fix` перед финальной проверкой

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_models.py` ✅ 19/19

---

## B-06 | 2026-03-17 | DONE

**Задача**: Drug Normalizer — сопоставление названий препаратов с реестром ГРЛС

**Что создано**:
- `backend/app/services/ocr/drug_normalizer.py` — класс `DrugNormalizer`:
  - `DrugMatch(drug_inn, display_name, is_rx, match_score)` — frozen dataclass
  - `normalize(drug_name: str) -> DrugMatch | None` — rapidfuzz WRatio, порог 80
  - `normalize_receipt_items(items: list[str]) -> list[DrugMatch | None]` — пакетная обработка
  - Кэш `{drug_name_lower: DrugMatch}` — 1000 повторных вызовов < 1 сек
  - `get_drug_normalizer()` — singleton factory
- `backend/tests/test_ocr/test_drug_normalizer.py` — 12 тестов: normalize, is_rx, cache, batch

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest` ✅ 12/12

**Следующие задачи** (зависимости выполнены):
- B-07 (OCR Pipeline Orchestrator, critical) — depends on B-03 ✅, B-04 ✅, B-05 ✅, B-06 ✅
- D-02 (Auth Router, critical) — depends on D-01 ✅
- D-03 (RLS Middleware, high) — depends on D-01 ✅, A-03 ✅

---

## D-02 | 2026-03-17 | DONE

**Задача**: Auth Router — 5 эндпоинтов аутентификации + исправление регрессии в OTPService

**Что создано**:
- `backend/app/schemas/auth.py` — Pydantic схемы: `OTPRequest` (regex +7XXXXXXXXXX), `VerifyRequest`, `BotRegisterRequest`, `MessageResponse`, `BotTokenResponse`
- `backend/app/routers/auth.py` — роутер `APIRouter(prefix="/auth")`:
  - `POST /auth/otp` — hash_phone(SHA-256) → поиск User → generate_otp → {message: 'Код отправлен'} (TODO: telegram_notifier в C-01)
  - `POST /auth/verify` — проверяет OTP → создаёт access+refresh JWT → httpOnly cookies (SameSite=strict, max_age 15min/30days)
  - `POST /auth/refresh` — декодирует refresh cookie → rotation → новые cookies
  - `POST /auth/logout` — delete_cookie access_token + refresh_token
  - `POST /auth/bot-register` — find_or_create User по telegram_id → JWT в теле ответа
- `backend/app/main.py` — зарегистрирован auth_router с prefix="/api/v1"
- `backend/tests/test_auth/test_auth_router.py` — 20 TDD тестов (SQLite StaticPool)

**Исправлено** (регрессия D-01):
- `backend/app/services/auth/otp_service.py` — убран лишний `raise HTTPException(429)` после инкремента `attempts`, теперь первые 5 попыток возвращают False, 6-я поднимает 429

**Решения**:
- phone_hash = SHA-256(phone) — детерминированный хеш для поиска в БД
- JWTService/OTPService инстанцируются как module-level синглтоны в роутере
- Тесты используют StaticPool для SQLite-in-memory (гарантия shared state между сессиями)

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_auth/` ✅ 43/43, полный suite ✅ 141/141

**Следующие задачи** (зависимости выполнены):
- C-01 (Bot Foundation, critical) — depends on A-01 ✅, A-05 ✅
- D-03 (RLS Middleware, high) — depends on D-01 ✅, A-03 ✅
- B-06 (Drug Normalizer, high) — depends on A-01 ✅

---

## B-07 | 2026-03-17 | DONE

**Задача**: OCR Pipeline Orchestrator — параллельный QR + EasyOCR + merge + normalize

**Что создано**:
- `backend/app/services/ocr/pipeline.py` — async `process_image(image_bytes) -> ParsedReceipt`:
  - `ParsedReceipt`: все поля MergedReceipt + items + processing_time_ms + ocr_status (DONE/REVIEW/FAILED)
  - `NormalizedItem`: drug_name_raw, drug_inn, is_rx, quantity, unit_price, total_price, drug_match_score
  - Параллельный запуск: `run_in_executor(scan_qr)` + `_run_easyocr` через asyncio.gather
  - ReceiptAgeEstimator: QR > 12 мес → qr_result=None
  - EasyOCR fallback: < 5 блоков → TesseractEngine; любое исключение → Tesseract
  - Логирование времени каждого шага (preprocess, qr+ocr, merge, normalize)
- `backend/app/services/ocr/qr_scanner.py`: pyzbar теперь lazy-imported внутри `_try_decode`
- `backend/tests/test_ocr/test_pipeline.py` — 12 тестов: age estimator, ocr_status, merged, ocr_only, both_failed, fallback, items, timing

**Верификация**: `ruff check` ✅ 0 ошибок, `pytest tests/test_ocr/` ✅ 74/74 (без qr_scanner — pyzbar не установлен в dev-env)

**Следующие задачи** (зависимости выполнены):
- D-02 (Auth Router, critical) — depends on D-01 ✅
- D-03 (RLS Middleware, high) — depends on D-01 ✅, A-03 ✅
- C-01 (Bot Foundation, critical) — depends on A-01 ✅, A-05 ✅

## 2026-03-18 C-01 Bot Foundation — DONE

Все файлы C-01 уже были реализованы ранее:
- bot/main.py: Application.builder().token(config.BOT_TOKEN).build(), polling/webhook режим
- bot/config.py: BOT_TOKEN, BACKEND_URL, WEBHOOK_URL, WEBHOOK_SECRET через pydantic-settings
- bot/services/api_client.py: BackendClient с httpx, cookie persistence, 401→refresh→retry
- bot/handlers/errors.py: ErrorHandler с Sentry + reply пользователю

Исправлен тест test_cookies_from_response_are_stored: хелпер _resp() теперь использует
list[tuple] для корректной передачи нескольких Set-Cookie заголовков вместо одного.

Результат: 12/12 тестов pytest tests/test_bot/test_api_client.py пройдено.

Следующие незаблокированные задачи (все deps=done): A-04, A-06, B-08, B-09, D-03, D-05, G-01, A-07

## 2026-03-18 D-03 RLS Middleware + get_current_user — DONE

Реализованы три компонента:
- app/middleware/rls.py: RLSMiddleware (BaseHTTPMiddleware), извлекает user_id из
  access_token cookie, пишет в request.state.current_user_id. Пропускает /api/v1/auth/* и /api/v1/health
- app/dependencies.py: get_current_user (401 при отсутствии/невалидном токене),
  get_current_user_optional (None вместо 401), get_db_rls (SET LOCAL app.current_user_id)
- app/main.py: RLSMiddleware зарегистрирован после CORS

Тесты: 13/13 прошли (py -3.11 -m pytest tests/test_middleware/test_rls.py)

Теперь незаблокированы: D-04 (RLS policies), E-01 (Receipts router), E-03 (Prescriptions router), I-01 (AES encryption)
